import 'package:flutter/material.dart';

class BookingCardHeaderWidget extends StatelessWidget {
  const BookingCardHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
